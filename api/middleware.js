// import handler from "./contact/handler";
// export const config = {
//   // Only run the middleware on the admin route
//   matcher: "/api",
// };

// export default function middleware(request) {
//   const url = new URL(request.url);
//   console.log(url);

//   return Response.redirect(url);
// }
